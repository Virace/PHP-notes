<?php
/**
 * 变量的声明及命名
 * @var integer
 */
    $age=20;
    // unset($age); //释放变量
    $bool=isset($age); //isset是否存在
    empty($age);//判断是否为空 真为空 0为空
    var_dump($bool); //打印变量信息 (文件位置，行数，变量类型，变量值)
 ?>

<pre>
  /**
   * 变量的声明及命名
   * @var integer
   */
      $age=20;
      // unset($age); //释放变量
      $bool=isset($age); //isset是否存在
      empty($age);//判断是否为空 真为空 0为空
      var_dump($bool); //打印变量信息 (文件位置，行数，变量类型，变量值)
</pre>
<a href="http://www.shucunwang.com/RunCode/php/" target="_blank">php在线测试</a>
