<?php
/**
 * boolean布尔型
 * @var boolean
 * false=非零、空字符串(空格为真)、一个0的字符串、空数组、没有对象的Object(此条为PHP4实用)、NULL
 */
          $bool=100;
          if($bool){
                    echo "真";
          }else{
                    echo "假";
          }


 ?>
